

echo "# prod-app" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/rafael46/prod-app.git
git push -u origin master


git add .

git commit -m "setting up repo"

git push

git add -A .    -->>   " is equivalent to "git add .; git add -u ."


se instalo boostrap@3

remove branch
git remote rm origin

# view current remotes
git remote -v
